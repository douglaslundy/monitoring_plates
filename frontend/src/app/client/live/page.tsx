import LiveMonitor from "@/components/live/LiveMonitor";

export default function ClientLivePage() {
  return (
    <LiveMonitor
      title="Live"
      description="Visualizacao ao vivo das cameras para confirmar funcionamento"
    />
  );
}
